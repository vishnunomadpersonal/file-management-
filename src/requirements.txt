fastapi==0.111.1
minio==7.2.7
sqlalchemy==2.0.31
pymysql==1.1.1
alembic==1.13.2
cryptography==43.0.0
celery[mysql]===5.4.0
kombu==5.3.7  # Advanced queue definitions for RabbitMQ
aiofiles===24.1.0
pytest===8.3.2
pytest-cov===5.0.0
aiohttp==3.9.5
starlette==0.37.2
httpx==0.27.0  # For async HTTP requests (virus scanning)

# Incremental ML Pipeline dependencies (pinned for reproducibility)
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.3.2
psutil==5.9.8
joblib==1.3.2

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
pydantic[email]==2.7.4

# Rate limiting
slowapi==0.1.9

# MCP Server (for document chat)
httpx==0.27.0
sse-starlette==2.1.0

# Input validation
email-validator==2.1.1

# Testing async
pytest-asyncio==0.23.5

# Chatbot - AI Providers
openai==1.35.0  # OpenAI API client (optional - only if using OpenAI)
# ollama is accessed via HTTP, no package needed

# LangChain for Text-to-SQL (production-ready SQL generation)
langchain==0.3.14
langchain-community==0.3.14

# DSPy - Automatic Prompt Optimizer (Stanford NLP)
# Discovers optimal prompts through training examples
dspy-ai==2.5.43
cachetools>=5.0.0  # Required by DSPy
cloudpickle>=3.0.0  # Required by DSPy
asyncer>=0.0.8  # Required by DSPy
structlog>=24.0.0  # Required by DSPy

# Redis - CRITICAL for production performance!
# Without Redis, every request hits the database directly
redis>=5.0.0  # High-performance caching layer

# Fast serialization for <10ms cached responses
msgpack>=1.0.0  # 5x faster than JSON serialization
orjson>=3.9.0  # 10x faster JSON response encoding

# Embedding model for hybrid intent classification (all-MiniLM-L6-v2)
# Using newer version compatible with huggingface_hub
sentence-transformers==3.0.1

# Docker SDK for Infrastructure Monitoring
# Note: Requires compatible urllib3/requests for Windows Docker named pipe support
docker==7.0.0
urllib3<2.0.0
requests<2.32.0

# ============================================================================
# Self-Healing AI System (Lightweight - no heavy dependencies)
# ============================================================================
# Uses existing NVIDIA LangChain integration for intelligent error detection
# and fixing. The self-healing system uses:
# - Rule-based error detection with ML-assisted analysis
# - Automatic pattern learning and fix generation
# - No CrewAI dependency (incompatible with current pydantic/openai versions)
langchain-openai
